# Images

Dépose ici ton portrait sous le nom `nicolas.jpg` (carré, 600×600 px minimum, WebP/JPG/PNG OK).

Si le fichier est absent, la page d'accueil affichera automatiquement les initiales `NG` en fallback.
